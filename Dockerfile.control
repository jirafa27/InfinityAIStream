FROM python:3.12-slim

ENV PIP_NO_CACHE_DIR=1 \
    DEBIAN_FRONTEND=noninteractive

WORKDIR /app

COPY requirements.telegram.txt ./
RUN pip install --no-cache-dir --default-timeout=120 -r requirements.telegram.txt

COPY . .
